const exportMe = () => {
    return "exported"
}

export default  b = 0 // just one value o func is permitted to be a default export in each module

export {exportMe} // export keyword allows us to import modules in other files js